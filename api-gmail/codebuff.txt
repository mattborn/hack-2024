create a vanilla HTML project in /gmail-pull with index.html, scripts.js, and styles.css that uses credentials.json to pull emails from Gmail, cache with IndexedDB, render with ag-grid, craft a brief README.md file that describes the project, keep the code as minimal as possible

create a node CLI script in /gmail-pull called pull.js that accepts a single argument for how many emails to pull and uses credentials.json to pull emails from Gmail and outputs a JSON file with the email data, craft a brief README.md file that describes the project, keep the code as minimal as possible
